using System.Collections.Generic;

public static class PlayerPrefsKeys
{
    /// <summary>
    /// If any UI open, disable all other game input functions if exist.
    /// <br/><br/>
    /// SetInt = 1 if exist, = 0 if not.
    /// </summary>
    public const string IS_UI_OPEN = "IS_UI_OPEN";
}
